# sevasmith — Team42

**Tech:** Docker, Groq, JWT, Supabase, TypeScript

## Context (read before the call)
- Взял на себя AI-компонент в команде — без опыта бэкенда и AI до старта проекта, всё с нуля
- Реализовал Supabase Edge Function с интеграцией Groq API, включая CORS handling, JWT-аутентификацию и защиту от prompt injection
- Провёл многодневный марафон отладки (включая сессии 16 часов подряд): CORS → 401 → обнаружил, что Supabase автоматически включает legacy JWT secret при деплое
- Параллельно с AI-компонентом реализовывал Dashboard-страницу: кастомные хуки, getDashboardStats, getTopicProgress, карточки статистики
- Команда использует Feature-Sliced Design (FSD) и GitFlow — всё это освоил с нуля за время проекта
- Ключевой личный вывод: попытка разобраться во всём бэкенде перед началом своей задачи — его главная ошибка, стоила несколько дней

## Architecture
- Расскажи об архитектуре вашего приложения в целом — как оно устроено, какие основные части есть и как они взаимодействуют?
- Ты отвечал за AI-компонент — объясни, как технически устроена эта часть: как запрос от пользователя доходит до AI и возвращается обратно?
- Почему для AI-интеграции выбрали именно Edge Functions, а не, например, отдельный бэкенд-сервис или прямые вызовы из фронтенда?
- Вы использовали Supabase — расскажи, какую роль он играет в проекте и какие его возможности задействовали?

## Hard Problem
- Расскажи про самый сложный технический баг за всё время проекта — как проявлялась проблема, что пробовал, как в итоге нашёл причину?
- Что такое CORS и почему это вообще стало проблемой в вашем проекте — как ты это понял и как решил?
- JWT-аутентификация — расскажи, как она работает в вашем проекте и какие сложности возникли при её настройке?

## Trade-offs
- Groq — почему именно этот провайдер? Смотрел ли на альтернативы, и чем Groq выигрывает или проигрывает по сравнению с ними?
- Если завтра количество пользователей вырастет в 100 раз — что в AI-компоненте сломается первым и почему?
- Что бы ты изменил в архитектуре AI-части, если бы начинал проект заново — есть решения, которые сейчас кажутся неоптимальными?

## Code Walk
- Опиши словами, как устроена твоя Edge Function — что она принимает на входе, какую логику выполняет, что возвращает?
- В AI-компонентах важна защита от манипуляций с промптами — как вы это решили в проекте, опиши подход?
- Расскажи, как устроена Dashboard-страница, которую ты делал — какие данные она показывает и как эти данные попадают в компоненты?

## Team Context
- Как в команде делили задачи — кто что делал, и как вы договаривались, кто берёт какую часть?
- Расскажи про code review в команде — как это было устроено, был ли момент, когда ревью помогло поймать что-то важное?
- Когда ты застрял на сложной проблеме — куда обращался за помощью: к команде, в документацию, к AI-ассистентам? Расскажи на конкретном примере.

## Reflection
- Шесть недель назад ты начинал без опыта в бэкенде и AI — что для тебя было самым неожиданным открытием за это время?
- Если бы тебе дали совет самому себе в начале проекта — что бы ты сказал, зная то, что знаешь сейчас?
- Что в вашем проекте ты считаешь по-настоящему хорошо сделанным — и почему именно это?
